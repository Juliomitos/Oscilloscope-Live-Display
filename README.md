# Oscilloscope-Live-Display
Python code for a Oscilloscope Live Display with a trigger function, voltage scaling, time scaling, and cvs data saving.
recieving signals and displaying it only works with it's paired C++ code inside the portable oscilloscope.
